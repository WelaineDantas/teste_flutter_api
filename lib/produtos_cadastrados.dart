import 'package:flutter/material.dart';

class ProdutosCadastrados extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Produtos"),
      ),
      body: Container(),
    );
  }
}
